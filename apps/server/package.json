{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "lint": "biome lint .",
    "typecheck": "tsc -b",
    "check-types": "pnpm run typecheck",
    "test": "echo \"No server tests (server is stub)\"",
    "dev": "echo 'API is in Tauri (desktop:dev)'",
    "start": "echo 'API is in Tauri'"
  },
  "dependencies": {},
  "devDependencies": {
    "@pickup/config": "workspace:*",
    "@types/node": "^22.13.14",
    "tsdown": "^0.16.5",
    "typescript": "catalog:"
  }
}
