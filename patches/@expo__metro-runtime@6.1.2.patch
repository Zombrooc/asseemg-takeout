diff --git a/src/location/install.native.ts b/src/location/install.native.ts
index 42e589f5e30b7e110aafc75bdf49150924022961..9387ceb0d8265935a88bee221013b97a701e495e 100644
--- a/src/location/install.native.ts
+++ b/src/location/install.native.ts
@@ -1,8 +1,7 @@
 // This MUST be first to ensure that `fetch` is defined in the React Native environment.
 import 'react-native/Libraries/Core/InitializeCore';
 
-// Ensure fetch is installed before adding our fetch polyfill to ensure Headers and Request are available globally.
-import 'whatwg-fetch';
+// React Native provides global fetch/Headers/Request; whatwg-fetch import removed to avoid pnpm resolution issues.
 // This MUST be imported to ensure URL is installed.
 import 'expo';
 // This file configures the runtime environment to increase compatibility with WinterCG.
